filebeat.inputs:
  - type: filestream
    id: app-logs
    paths:
      - /logs/app.log

processors:
  - add_docker_metadata:
      host: 'unix:///var/run/docker.sock'

output.elasticsearch:
  hosts: ['http://elasticsearch:9200']
