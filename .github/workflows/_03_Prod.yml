name: 03 - Deploy OVH

on:
  push:
    branches:
      - main

jobs:
  Deploy_OVH:
    uses: ./.github/workflows/03-1_Deploy.yml
    permissions:
      contents: read
      packages: write
      id-token: write
